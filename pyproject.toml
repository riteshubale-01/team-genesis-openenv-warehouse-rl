[project]
name = "warehouse-rl"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn"
]

[project.scripts]
server = "server.app:app"

[tool.openenv]
mode = "multi"
